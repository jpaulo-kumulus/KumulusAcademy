import deck

cards = deck.create_deck()

for card in cards:
  print(card)

# test para importar **********************************************************************************

import decktest

cards = decktest.create_deck_test()

for card in cards:
  print(card)